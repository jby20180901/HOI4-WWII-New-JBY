# befriend
# conquer
# antagonize
# invade
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

any_country_no_ally_man = {
	enable = {
		OR ={
			has_government = fascism
			has_government = neutrality
		}
		NOT = {
			tag = MAN
			tag = JAP
		}
	}
	abort = {
		OR = {
			MAN = { exists = no }
			MAN = { NOT = { has_idea = MAN_Low_legitimism } }
			MAN = { has_government = democratic }
			is_in_faction_with = MAN
			has_war_together_with = MAN
		}
	}
	
	ai_strategy = {
		type = alliance
		id = "MAN"			
		value = -100
	}	
}
man_ally_ger_is_difficult = {
	enable = {
		tag = MAN
	}
	abort = {
		OR = {
			MAN = { NOT = { has_idea = MAN_Low_legitimism } }
			GER = { is_in_faction_with = MAN }
			GER = { has_war_together_with = MAN }
			GER = { has_completed_focus = GER_japan_friend }
		}
	}

	ai_strategy = {
		type = alliance
		id = GER			
		value = -100
	}
}
man_ally_ita_is_difficult = {
	enable = {
		tag = MAN
	}
	abort = {
		OR = {
			MAN = { NOT = { has_idea = MAN_Low_legitimism } }
			ITA = { is_in_faction_with = MAN }
			ITA = { has_war_together_with = MAN }
			ITA = { has_completed_focus = ITA_japan_friend }
		}
	}

	ai_strategy = {
		type = alliance
		id = ITA			
		value = -100
	}
}