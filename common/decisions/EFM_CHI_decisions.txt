
CHI_Military_affairs_commission_resolution = {
	
	CHI_Kaifeng_military_conference = {

		icon = generic_nationalism

		available = {
			OR = {
					has_global_flag = CHI_Hold_military_meeting
					AND = {
							any_other_country = {
									has_war_with = CHI
									controls_state = 608
									controls_state = 614
									controls_state = 597
								}
						}
					AND = {
							surrender_progress > 0.3
						}
					AND = {
							threat > 0.3
						}
				}
		}

		cost = 10
		ai_will_do = {
			factor = 500
		}
		days_remove = 15

		visible = {
			has_idea = CHI_Victor_central_plains_war
		}
		remove_effect = {
			if = {
				limit = { has_idea = CHI_Victor_central_plains_war }
				swap_ideas = {
					remove_idea = CHI_Victor_central_plains_war
					add_idea = CHI_Inspired_army
				}
			}
			add_war_support = 0.1
			add_stability = 0.05
		}
		complete_effect = {
					
		}		
	}
	
	CHI_Rectify_the_army = {

		icon = generic_prepare_civil_war

		available = {
			has_army_experience > 399
		}

		cost = 100
		ai_will_do = {
			factor = 500
		}
		days_remove = 90

		visible = {
			has_idea = CHI_Inspired_army
		}
		remove_effect = {
			if = {
				limit = { has_idea = CHI_Inspired_army }
				swap_ideas = {
					remove_idea = CHI_Inspired_army
					add_idea = CHI_Army_innovation
				}
			}
		}
		complete_effect = {
			army_experience = -400
		}		
	}
	
	CHI_Transfer_capital = {

		icon = generic_prepare_civil_war

		available = {
		    CHI = { is_ai = no }
			event_target:WTT_current_china_leader = {
					controls_state = 620
				}
			any_other_country = {
					num_of_factories > 50
					has_war_with = event_target:WTT_current_china_leader
				}
		}

		fire_only_once = yes
		cost = 25
		
		ai_will_do = {
			factor = 500
		}
		days_remove = 35

		visible = {
			tag = event_target:WTT_current_china_leader
			event_target:WTT_current_china_leader = {
					controls_state = 620
				}
			any_other_country = {
					num_of_factories > 50
					has_war_with = event_target:WTT_current_china_leader
				}
			NOT = {
					event_target:WTT_current_china_leader = {
							NOT = { controls_state = 598 }
						}
				}
		}
		cancel_trigger = { #will abort
			event_target:WTT_current_china_leader = {
					OR = {
						NOT = { controls_state = 598 }
						NOT = { controls_state = 613 }
					}
				}
		}
		remove_effect = {
			set_capital = 620
			add_timed_idea = {
					idea = CHI_Transfer_capital
					days = 365
				}
			add_stability = 0.05
			add_ideas = CHI_National_government_austerity
			598 = {
					remove_building = {
							type = industrial_complex
							level = 2
							instant_build = yes
						}
				}
			613 = {
					remove_building = {
							type = industrial_complex
							level = 2
							instant_build = yes
						}
				}
			620 = {
					add_extra_state_shared_building_slots = 4
					add_building_construction = {
							type = industrial_complex
							level = 4
							instant_build = yes
						}
				}
			set_country_flag = CHI_Transfer_capital_now
			custom_effect_tooltip = CHI_Transfer_capital_tooltip
			custom_effect_tooltip = CHI_Transfer_capital_tooltip2
			hidden_effect = {
					country_event = {
							days = 1
							id = news.1006
						}
				}
		}
		complete_effect = {
				
		}		
	}
	CHI_Wartime_capital = {

		icon = generic_prepare_civil_war

		available = {
			event_target:WTT_current_china_leader = {
					controls_state = 605
					NOT = {
							has_idea = CHI_Transfer_capital
						}
				}
			any_other_country = {
					num_of_factories > 50
					has_war_with = event_target:WTT_current_china_leader
				}
		}

		fire_only_once = yes
		cost = 50
		
		ai_will_do = {
			factor = 8
		}
		days_remove = 70

		visible = {
			has_country_flag = CHI_Transfer_capital_now
		}
		cancel_trigger = { #will abort
			event_target:WTT_current_china_leader = {
					NOT = { controls_state = 605 }
				}
		}
		remove_effect = {
					set_capital = 605
					add_stability = 0.05
					set_country_flag = CHI_Wartime_capital_now
					hidden_effect = {
							news_event = {
									hours = 6
									id = news.1041
								}
						}
					if = {
							limit = {
									NOT = {
											has_country_flag = CHI_evacuated_from_wuhan
										}
								}
							620 = {
									remove_building = {
											type = industrial_complex
											level = 4
											instant_build = yes
										}
								}
							605 = {
									add_extra_state_shared_building_slots = 4
									add_building_construction = {
											type = industrial_complex
											level = 4
											instant_build = yes
										}
								}
						}
		}
		complete_effect = {
				
		}		
	}
	
	CHI_Chongqing_auxiliary_capital = {

		icon = generic_prepare_civil_war

		available = {
			is_subject = no
			OR = {
					any_other_country = {
							num_of_factories > 50
							has_war_with = event_target:WTT_current_china_leader
						}
					threat > 0.3
				}
		}

		fire_only_once = yes
		cost = 150
		
		ai_will_do = {
			factor = 8
		}
		days_remove = 70

		visible = {
			has_country_flag = CHI_Wartime_capital_now
		}
		cancel_trigger = { #will abort
			
		}
		remove_effect = {
					605 = {
							add_extra_state_shared_building_slots = 5
							add_building_construction = {
									type = industrial_complex
									level = 3
									instant_build = yes
								}
							add_building_construction = {
									type = arms_factory
									level = 2
									instant_build = yes
								}
						}
		}
		complete_effect = {
				
		}		
	}
	
	CHI_Restore_capital = {

		icon = generic_prepare_civil_war

		available = {
			NOT = {
					any_other_country = {
							num_of_factories > 50
							has_war_with = event_target:WTT_current_china_leader
						}
				}
			event_target:WTT_current_china_leader = {
					controls_state = 613
				}
		}

		fire_only_once = yes
		cost = 25
		
		ai_will_do = {
			factor = 8
		}
		days_remove = 70

		visible = {
			has_country_flag = CHI_Wartime_capital_now
		}
		cancel_trigger = { #will abort
			
		}
		remove_effect = {
			set_capital = 613
		}
		complete_effect = {
				
		}		
	}
	
	
	CHI_Operation_Yellow_River = {

		icon = generic_prepare_civil_war

		available = {
			is_subject = no
			event_target:WTT_current_china_leader = {
					controls_state = 607
				}
			any_other_country = {
					num_of_factories > 50
					has_offensive_war_with = event_target:WTT_current_china_leader
				}
		}

		fire_only_once = yes
		cost = 100
		
		ai_will_do = {
				factor = 1
			}
		days_remove = 70

		visible = {
			tag = event_target:WTT_current_china_leader
			is_subject = no
			event_target:WTT_current_china_leader = {
					controls_state = 607
				}
			any_other_country = {
					num_of_factories > 50
					has_offensive_war_with = event_target:WTT_current_china_leader
				}
		}
		cancel_trigger = { #will abort
			NOT = {
					event_target:WTT_current_china_leader = {
							controls_state = 607
						}
				}
		}
		remove_effect = {
			607 = {
					remove_building = {
							type = infrastructure
							level = 3
							instant_build = yes
						}
				}
			606 = {
					remove_building = {
							type = infrastructure
							level = 1
							instant_build = yes
						}
				}
			597 = {
					remove_building = {
							type = infrastructure
							level = 2
							instant_build = yes
						}
				}
			598 = {
					remove_building = {
							type = infrastructure
							level = 2
							instant_build = yes
						}
				}
		}
		complete_effect = {
				
		}		
	}
	
	CHI_Material_deployment = {

		icon = generic_prepare_civil_war

		available = {
			is_subject = no
			any_other_country = {
					num_of_factories > 50
					has_offensive_war_with = event_target:WTT_current_china_leader
				}
		}

		fire_only_once = yes
		cost = 25
		
		ai_will_do = {
			factor = 5
		}
		days_remove = 0

		visible = {
			tag = event_target:WTT_current_china_leader
			is_subject = no
			any_other_country = {
					num_of_factories > 50
					has_offensive_war_with = event_target:WTT_current_china_leader
				}
		}
		cancel_trigger = { #will abort
			
		}
		remove_effect = {
			
		}
		complete_effect = {
			if = {
					limit = {
							is_faction_leader = yes
						}
					country_event = {
							days = 0
							id = EMF_china.260
						}
				}
			if = {
					limit = {
							is_faction_leader = no
							is_in_faction = yes
						}
					add_ideas = CHI_Material_deployment_all
				}
			if = {
					limit = {
							is_in_faction = no
						}
					add_ideas = CHI_Material_deployment_all
				}
		}		
	}
	
	CHI_Shrink_the_navy_department = {
		icon = generic_naval
		fire_only_once = yes
		cost = 10
		ai_will_do = {
			factor = 0
			modifier = {
				add = 500
				any_other_country = {
						num_of_factories > 50
						has_offensive_war_with = CHI
					}
			}
		}

		complete_effect = {
			add_ideas = CHI_Shrink_the_navy_department
		}		
	}
	CHI_Restart_Navy_program = {
		icon = generic_naval
		fire_only_once = yes
		available = {
			has_idea = CHI_Shrink_the_navy_department
		}

		cost = 150
		ai_will_do = {
			factor = 0
			modifier = {
				add = 500
				has_defensive_war = no
			}
		}
		days_remove = 90

		visible = {
			has_idea = CHI_Shrink_the_navy_department
		}
		remove_effect = {
			remove_ideas = CHI_Shrink_the_navy_department
		}
	}
	CHI_Kunming_aviation_school = {
		icon = generic_air
		fire_only_once = yes

		cost = 50
		ai_will_do = {
			factor = 10
		}
		days_remove = 70

		visible = {
			has_completed_focus = CHI_guyongchennade
		}
		remove_effect = {
			add_tech_bonus = {
					name = airforce_bonus
					bonus = 0.5
					uses = 1
					category = air_doctrine
				}
		}
	}
	
	CHI_Defense_Wuhan = {
		icon = generic_prepare_civil_war
		fire_only_once = yes
		cost = 50
		ai_will_do = {
			factor = 1
			modifier = {
				add = 50
				any_other_country = {
						num_of_factories > 50
						has_offensive_war_with = CHI
					}
				surrender_progress > 0.4
			}
		}
		days_remove = 70

		visible = {
			has_completed_focus = CHI_guofang_xian
		}
		available = {
			controls_state = 620
		}
		remove_effect = {
			620 = {
					add_building_construction = {
							type = bunker
							level = 3
							province = 4619
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 4631
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 1036
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 7637
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 9959
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 7161
							instant_build = yes
						}
				}
		}
	}
	CHI_Chongqing_defense_line = {
		icon = generic_prepare_civil_war
		fire_only_once = yes
		cost = 50
		ai_will_do = {
			factor = 1
			modifier = {
				add = 500
				any_other_country = {
						num_of_factories > 50
						has_offensive_war_with = CHI
					}
				surrender_progress > 0.6
			}
		}
		days_remove = 70

		visible = {
			has_completed_focus = EastAsia_CHI_guofuqianduyukangzhanqiantu
		}
		available = {
			controls_state = 605
			surrender_progress > 0.6
		}
		remove_effect = {
			605 = {
					add_building_construction = {
							type = bunker
							level = 3
							province = 6999
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 11972
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 4431
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 4141
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 1893
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 11865
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 2030
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 12274
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 7256
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 7240
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 8026
							instant_build = yes
						}
					add_building_construction = {
							type = bunker
							level = 2
							province = 12767
							instant_build = yes
						}
				}
		}
	}
    CHI_beijiaer = {

		icon = generic_prepare_civil_war
        fire_only_once = yes
		available = {
			has_completed_focus = CHI_xiangbeijiaerjinjun
		}

		cost = 0
		ai_will_do = {
			factor = 500
		}
		days_remove = 1

		visible = {
            has_completed_focus = CHI_xiangbeijiaerjinjun
		}
		remove_effect = {
            
		}
		complete_effect = {
			create_wargoal = {
					type = take_state
					target = SOV
                    generator = { 657 561 560 409 408 }
				}
            unlock_decision_tooltip = CHI_beijiaer_victory
		}		
	}
    CHI_beijiaer_victory = {

		icon = generic_prepare_civil_war
        fire_only_once = yes
		available = {
			has_completed_focus = CHI_xiangbeijiaerjinjun
            controls_province = 7835
		}

		cost = 0
		ai_will_do = {
			factor = 500
		}
		days_remove = 1

		visible = {
            has_completed_focus = CHI_xiangbeijiaerjinjun
		}
		remove_effect = {
            
		}
		complete_effect = {
            if = {
                limit = {
                    SOV = {
                        controls_state = 537
                    }
                }
                CHI = { 
                    transfer_state = 657
                    transfer_state = 561
                    transfer_state = 560
                    transfer_state = 409
                    transfer_state = 408
                    transfer_state = 655
                    transfer_state = 563
                    transfer_state = 564
                    transfer_state = 537
                }
                SOV = {
                    white_peace = CHI
                }
            }
            else = {
                CHI = { 
                    transfer_state = 657
                    transfer_state = 561
                    transfer_state = 560
                    transfer_state = 409
                    transfer_state = 408
                    transfer_state = 655
                    transfer_state = 563
                    transfer_state = 564
                }
                SOV = {
                    white_peace = CHI
                }
            }
		}		
	}

}


# Diplomatic relations with Germany

CHI_Diplomatic_relations_with_germany = {

	CHI_Invitation_Germany_to_mediation = {
		icon = generic_political_discourse
		fire_only_once = yes
		days_remove = 70 # Stays for 5 days before being removed
		cost = 0
		
		target_trigger = {
			FROM = { tag = GER }
		}
		visible = { #can see
			has_war_with = JAP
			JAP = {
					NOT = {
							has_war_with = GER
						}
					has_government = fascism
				}
			GER = {
					has_government = fascism
				}
			NOT = {
					has_war_with = GER
					has_government = communism
				}
		}
		available = { #can do
			has_war_with = JAP
			JAP = {
					NOT = {
							has_war_with = GER
						}
					has_government = fascism
				}
			GER = {
					has_government = fascism
				}
			NOT = {
					has_war_with = GER
					has_government = communism
				}
		}
		complete_effect = { #run effect on selection
	 
		}
		remove_trigger = { #will run remove effect and abort
	
		}
		remove_effect = { #run effect when removed/finished
			GER = {
					country_event = {
							id = EMF_china.285
							days = 1
						}
				}
		}
		cancel_trigger = { #will abort
			has_war_with = GER
			NOT = {
					has_war_with = JAP
				}
		}
		ai_will_do = {
			factor = 10
		}
	}

	CHI_HAPRO = {
		icon = generic_political_discourse
		fire_only_once = yes
		days_remove = 5 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = GER }
		}
		visible = { #can see
			NOT = {
					has_war_with = GER
					has_government = communism
				}
		}
		available = { #can do
			OR = {
					is_subject = no
					is_subject_of = GER
				}
			GER = {
					exists = yes
					is_puppet = no
				}
			NOT = {
					has_war_with = GER
					GER = {
							has_completed_focus = GER_deriyouhao
							has_government = communism
						}
					has_government = communism
				}
		}
		complete_effect = { #run effect on selection
	
		}
		remove_trigger = { #will run remove effect and abort
	
		}
		remove_effect = { #run effect when removed/finished
			country_event = {
					id = EMF_china.53
					days = 1
				}
		}
		cancel_trigger = { #will abort
			has_war_with = GER
		}
		ai_will_do = {
			factor = 10
		}
	} 
	
	CHI_Affect_German_diplomacy = {
		icon = generic_political_discourse
		fire_only_once = yes
		days_remove = 30 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = GER }
		}
		visible = { #can see
			GER = { has_opinion = { target = ROOT value < 75 } }
			NOT = {
					has_war_with = GER
					has_government = communism
				}
		}
		available = { #can do
			OR = {
					is_subject = no
					is_subject_of = GER
				}
			GER = {
					exists = yes
					is_puppet = no
				}
			NOT = {
					has_war_with = GER
					GER = {
							has_completed_focus = GER_deriyouhao
							has_government = communism
						}
					has_government = communism
				}
		}
		remove_effect = { #run effect when removed/finished
			GER = {
					country_event = {
							days = 1
							id = EMF_china.59
						}
				}
		}
		cancel_trigger = { #will abort
			has_war_with = GER
		}
		ai_will_do = {
			factor = 0.1
		}
	}
	
}

# Diplomatic relations with Japan

CHI_Diplomatic_relations_with_japan = {

	CHI_Invite_japanese_military_advisers = {
		icon = generic_tank
		days_re_enable = 30
		days_remove = 60
		cost = 25
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = {
			JAP = {
				exists = yes
			}
			NOT = { has_war_with = JAP }
		}
		modifier = { #active during the course of the decision
			experience_gain_army = 0.05
		}
		cancel_trigger = {
			OR = {
				has_war_with = JAP
				JAP = {
					exists = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Inviting_japan_assist_air_force_development = {
		icon = generic_air
		days_re_enable = 30
		days_remove = 60
		cost = 50
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = {
			JAP = {
				exists = yes
			}
			NOT = { has_war_with = JAP }
		}
		modifier = { #active during the course of the decision
			experience_gain_air = 0.05
		}
		cancel_trigger = {
			OR = {
				has_war_with = JAP
				JAP = {
					exists = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Inviting_japan_assist_navy_development = {
		icon = generic_naval
		days_re_enable = 30
		days_remove = 60
		cost = 75
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = {
			JAP = {
				exists = yes
			}
			NOT = { has_war_with = JAP }
		}
		modifier = { #active during the course of the decision
			experience_gain_navy = 0.05
		}
		cancel_trigger = {
			OR = {
				has_war_with = JAP
				JAP = {
					exists = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Invite_japanese_army_to_perform_simulated_war = {
		icon = generic_tank
		days_re_enable = 90
		days_remove = 14
		cost = 50
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = {
			JAP = {
				exists = yes
			}
			NOT = { has_war_with = JAP }
		}
		remove_effect = {
			army_experience = 25
		}
		cancel_trigger = {
			OR = {
				has_war_with = JAP
				JAP = {
					exists = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Request_to_dissolve_mengkukuo = {
		icon = generic_political_discourse
		fire_only_once = yes
		days_remove = 14
		cost = 25
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = {
			JAP = {
				exists = yes
			}
			MEN = {
				exists = yes
				is_subject_of = JAP
			}
			NOT = { has_war_with = JAP }
		}
		available = { #can do
			NOT = { has_war_together_with = MEN }
		}
		remove_effect = {
			JAP = { country_event = { id = EMF_china.167 hours = 12 } }
		}
		cancel_trigger = {
			OR = {
				has_war_with = JAP
				MEN = {
					exists = no
				}
				MEN = {
					is_subject = no
				}
			}
		}
		ai_will_do = {
			factor = 500
		}
	}

}


CHI_Japanese_democratic_civil_war = {

	CHI_Give_guarantee_for_democratic_japan = {
		icon = generic_nationalism
		fire_only_once = yes
		days_remove = 5 # Stays for 5 days before being removed
		cost = 10
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = democratic
				has_civil_war = yes
			}
			NOT = { has_war_with = JAP }
		}
		available = { #can do
			JAP = {
				has_government = democratic
				has_civil_war = yes
			}
			NOT = { has_war_with = JAP }
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			give_guarantee = JAP
			JAP = {
				add_ai_strategy = {
						type = befriend
						id = "CHI"
						value = 200
					}
			}
		}
		cancel_trigger = { #will abort
			OR = {
				has_war_with = JAP
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Financial_support_japanese_democrats = {
		icon = generic_prepare_civil_war
		fire_only_once = yes
		days_remove = 35 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = democratic
				has_civil_war = yes
			}
			NOT = { has_war_with = JAP }
		}
		available = { #can do
			JAP = {
				has_government = democratic
				has_civil_war = yes
			}
			NOT = { has_war_with = JAP }
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			JAP = { country_event = { id = EMF_china.351 hours = 12 } }
		}
		cancel_trigger = { #will abort
			OR = {
				has_war_with = JAP
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	CHI_Military_support_japanese_democrats = {
		icon = generic_prepare_civil_war
		fire_only_once = yes
		days_remove = 35 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = democratic
				has_civil_war = yes
			}
			NOT = { has_war_with = JAP }
		}
		available = { #can do
			infantry_equipment > 499
			JAP = {
				has_government = democratic
				has_civil_war = yes
			}
			NOT = { has_war_with = JAP }
		}
		complete_effect = { #run effect on selection
			effect_tooltip = {
					ROOT = {
						send_equipment = { target = JAP type = infantry_equipment amount = 500 }
					}
				}
		}
		remove_effect = { #run effect when removed/finished
			JAP = { country_event = { id = EMF_china.352 hours = 12 } }
		}
		cancel_trigger = { #will abort
			OR = {
				has_war_with = JAP
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	CHI_Interference_in_japanese_democratic_civil_war = {
		icon = generic_operation
		fire_only_once = yes
		days_remove = 70 # Stays for 5 days before being removed
		cost = 100
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = democratic
				has_civil_war = yes
			}
			NOT = { has_war_with = JAP }
		}
		available = { #can do
			JAP = {
				has_government = democratic
				has_civil_war = yes
				surrender_progress > 0.5
			}
			NOT = { has_war_with = JAP }
			MAN = {
				OR = {
					exists = no
					is_subject_of = ROOT
					has_war_with = ROOT
				}
			}
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			JAP = { country_event = { id = EMF_china.354 hours = 12 } }
			every_other_country = {
					limit = {
							original_tag = JAP
							NOT = { has_government = democratic }
						}
					country_event = { id = EMF_china.353 hours = 12 }
				}
		}
		cancel_trigger = { #will abort
			OR = {
				has_war_with = JAP
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
}

CHI_Interference_in_the_japanese_civil_war = {

	CHI_Give_guarantee_for_japanese_government = {
		icon = generic_nationalism
		fire_only_once = yes
		days_remove = 5 # Stays for 5 days before being removed
		cost = 10
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					NOT = { has_war_with = ROOT }
				}
		}
		available = { #can do
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					NOT = { has_war_with = ROOT }
				}
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			every_other_country = {
					limit = {
							original_tag = JAP
							NOT = { has_government = communism }
						}
					ROOT = { give_guarantee = PREV }
					add_ai_strategy = {
							type = befriend
							id = "CHI"
							value = 200
						}
				}
			JAP = { country_event = { id = EMF_china.355 hours = 12 } }
		}
		cancel_trigger = { #will abort
			OR = {
				any_other_country = {
						original_tag = JAP
						NOT = { has_government = communism }
						has_war_with = ROOT
					}
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Financial_support_japanese_government = {
		icon = generic_prepare_civil_war
		fire_only_once = yes
		days_remove = 35 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					NOT = { has_war_with = ROOT }
				}
		}
		available = { #can do
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					NOT = { has_war_with = ROOT }
				}
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			every_other_country = {
					limit = {
							original_tag = JAP
							NOT = { has_government = communism }
						}
					add_ideas = CHI_Financial_support_the_japanese_democrats
				}
			JAP = { country_event = { id = EMF_china.356 hours = 12 } }
		}
		cancel_trigger = { #will abort
			OR = {
				any_other_country = {
						original_tag = JAP
						NOT = { has_government = communism }
						has_war_with = ROOT
					}
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Military_support_japanese_government = {
		icon = generic_prepare_civil_war
		fire_only_once = yes
		days_remove = 35 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					NOT = { has_war_with = ROOT }
				}
		}
		available = { #can do
			infantry_equipment > 499
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					surrender_progress > 0.25
					NOT = { has_war_with = ROOT }
				}
		}
		complete_effect = { #run effect on selection
			effect_tooltip = {
					ROOT = {
						send_equipment = { target = JAP type = infantry_equipment amount = 500 }
					}
				}
		}
		remove_effect = { #run effect when removed/finished
			every_other_country = {
					limit = {
							original_tag = JAP
							NOT = { has_government = communism }
						}
					add_equipment_to_stockpile = { type = infantry_equipment_0 amount = 500 producer = CHI }
				}
			JAP = { country_event = { id = EMF_china.357 hours = 12 } }
		}
		cancel_trigger = { #will abort
			OR = {
				any_other_country = {
						original_tag = JAP
						NOT = { has_government = communism }
						has_war_with = ROOT
					}
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 10
		}
	}
	CHI_Interference_in_japanese_communist_revolution = {
		icon = generic_operation
		fire_only_once = yes
		days_remove = 70 # Stays for 5 days before being removed
		cost = 100
		
		target_trigger = {
			FROM = { tag = JAP }
		}
		visible = { #can see
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					NOT = { has_war_with = ROOT }
				}
		}
		available = { #can do
			JAP = {
				has_government = communism
				has_civil_war = yes
			}
			any_other_country = {
					original_tag = JAP
					NOT = { has_government = communism }
					surrender_progress > 0.6
					NOT = { has_war_with = ROOT }
				}
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			every_other_country = {
					limit = {
							original_tag = JAP
							NOT = { has_government = communism }
						}
					country_event = { id = EMF_china.358 hours = 12 }
				}
			add_ai_strategy = {
					type = conquer
					id = "JAP"
					value = 200
				}
		}
		cancel_trigger = { #will abort
			OR = {
				any_other_country = {
						original_tag = JAP
						NOT = { has_government = communism }
						has_war_with = ROOT
					}
				JAP = {
					has_civil_war = no
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
}

CHI_The_manchukou_question = {
	CHI_Issue_an_ultimatum_with_manchukou = {
		icon = generic_operation
		fire_only_once = yes
		days_remove = 3 # Stays for 5 days before being removed
		cost = 10
		
		target_trigger = {
			FROM = { tag = MAN }
		}
		visible = { #can see
			MAN = {
				exists = yes
				is_subject = no
				NOT = {
						has_war_with = ROOT
						is_in_faction_with = ROOT
					}
			}
		}
		available = { #can do
			MAN = {
				is_subject = no
				NOT = { has_war_with = ROOT }
			}
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			country_event = {
					id = EMF_china.152
				}
		}
		cancel_trigger = { #will abort
			MAN = {
					OR = {
							exists = no
							is_subject_of = ROOT
							has_war_with = ROOT
							is_in_faction_with = ROOT
						}
				}
		}
		ai_will_do = {
			factor = 200
			modifier = {
				add = -100
				OR = {
					any_other_country = {
							is_in_faction_with = MAN
							strength_ratio = {
								tag = ROOT
								ratio > 0.75
							}
						}
					MAN = {
							strength_ratio = {
								tag = ROOT
								ratio > 0.75
							}
						}
				}
			}
		}
	}
	
	CHI_Recognize_the_manchukou = {
		icon = generic_operation
		fire_only_once = yes
		days_remove = 70 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = MAN }
		}
		visible = { #can see
			MAN = {
				exists = yes
				NOT = { has_war_with = ROOT }
				NOT = {
						has_war_with = ROOT
						is_subject_of = ROOT
					}
			}
		}
		available = { #can do
			MAN = {
				is_subject = no
				NOT = { has_war_with = ROOT }
			}
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			add_opinion_modifier = {
				target = MAN
				modifier = Nations_friendly
			}
			add_ai_strategy = {
					type = befriend
					id = "CHI"
					value = 100
				}
			MAN = {
				country_event = {
						id = EMF_china.164
					}
			}
		}
		cancel_trigger = { #will abort
			MAN = {
					OR = {
							exists = no
							is_subject_of = ROOT
							has_war_with = ROOT
							is_in_faction_with = ROOT
						}
				}
		}
		ai_will_do = {
			factor = 100
			modifier = {
				add = 100
				OR = {
					any_other_country = {
							is_in_faction_with = MAN
							strength_ratio = {
								tag = ROOT
								ratio > 0.75
							}
						}
					MAN = {
							strength_ratio = {
								tag = ROOT
								ratio > 0.75
							}
						}
				}
			}
		}
	}
}

foreign_politics = {

	CHI_Military_procurement_with_italy = {
		icon = generic_tank
		days_re_enable = 120
		days_remove = 14 # Stays for 5 days before being removed
		cost = 50
		
		target_trigger = {
			FROM = { tag = ITA }
		}
		visible = { #can see
			has_global_flag = CHI_sino_ita_trade_agreement
			owns_state = 613
			NOT = { has_war_with = ITA }
		}
		available = { #can do
			NOT = { has_war_with = ITA }
		}
		complete_effect = { #run effect on selection
	
		}
		remove_effect = { #run effect when removed/finished
			country_event = { id = EMF_china.40 }
		}
		cancel_trigger = { #will abort
			has_war_with = ITA
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	JAP_Invite_china_to_attack_soviet_union = {
		icon = generic_operation
		fire_only_once = yes
		days_remove = 5
		cost = 0
		
		target_trigger = {
			FROM = {
				tag = CHI
			}
		}
		visible = {
			tag = JAP
			is_subject = no
			has_global_flag = CHI_Sino_japanese_cooperation
			has_war_with = SOV
			NOT = { has_war_with = CHI }
			CHI = {
				is_subject = no
				NOT = {
						has_government = communism
						has_war_with = SOV
						is_in_faction_with = SOV
						has_war_together_with = SOV
					}
				}
		}
		remove_effect = {
			CHI = { country_event = { id = EMF_china.169 } }
		}
		cancel_trigger = {
			CHI = {
				OR = {
					exists = no
					is_subject = yes
					has_government = communism
					has_war_with = JAP
					has_war_with = SOV
					is_in_faction_with = SOV
					has_war_together_with = SOV
				}
			}
		}
		ai_will_do = {
			factor = 500
		}
	}
	JAP_Japan_asks_to_join_us_against_soviet_union = {
		icon = generic_operation
		fire_only_once = yes
		days_remove = 5
		cost = 0
		
		target_trigger = {
			FROM = {
				tag = CHI
			}
		}
		visible = {
			tag = JAP
			is_subject = no
			has_global_flag = CHI_Sino_japanese_cooperation
			NOT = { has_war_with = SOV }
			NOT = { has_war_with = CHI }
			CHI = {
					is_subject = no
					has_war_with = SOV
				}
		}
		remove_effect = {
			CHI = { country_event = { id = EMF_china.170 } }
		}
		cancel_trigger = {
			CHI = {
				OR = {
					exists = no
					is_subject = yes
					has_government = communism
					has_war_with = JAP
					NOT = { has_war_with = SOV }
				}
			}
		}
		ai_will_do = {
			factor = 500
		}
	}
	
}

political_actions = {
	CHI_Integrate_the_local_force = {
		icon = generic_prepare_civil_war
		available = {
			FROM = { compare_autonomy_state < autonomy_colony }
		}

		visible = {
			OR = {
				is_literally_china = yes
				tag = XBJ
			}
			num_subjects > 0
			FROM = {
				is_subject_of = ROOT
				compare_autonomy_state > autonomy_integrated_puppet
			}
		}
		cancel_if_not_visible = yes

		cost = 200

		target_trigger = {
			FROM = {
				is_subject_of = ROOT
				compare_autonomy_state > autonomy_integrated_puppet
				OR = {
					is_literally_china = yes
					tag = XBJ
				}
			}
		}

		days_remove = 90
		fire_only_once = yes
		ai_will_do = {
			factor = 10
			modifier = {
				FROM = {
					is_ai = no
				}
				factor = 0
			}
		}

		remove_effect = {
			FROM = {
				transfer_navy = {
					target = ROOT
				}
			}
			annex_country = { target = FROM transfer_troops = yes }
		}
	}
	CHI_Integrate_the_local_force_2 = {
		icon = generic_prepare_civil_war
		available = {
			FROM = { compare_autonomy_state < autonomy_puppet }
		}

		visible = {
			OR = {
				is_literally_china = yes
				tag = XBJ
			}
			num_subjects > 0
			FROM = { is_subject_of = ROOT }
		}
		cancel_if_not_visible = yes

		cost = 100

		target_trigger = {
			FROM = {
				is_subject_of = ROOT
				compare_autonomy_state < autonomy_puppet
				OR = {
					is_literally_china = yes
					tag = XBJ
				}
			}
		}

		days_remove = 90
		fire_only_once = yes
		ai_will_do = {
			factor = 10
			modifier = {
				FROM = {
					is_ai = no
				}
				factor = 0
			}
		}

		remove_effect = {
			FROM = {
				transfer_navy = {
					target = ROOT
				}
			}
			annex_country = { target = FROM transfer_troops = yes }
		}
	}
}