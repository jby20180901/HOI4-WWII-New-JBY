
ENG_Intervene_persian = {

	ENG_Ask_persian_to_expel_germans = {

		icon = generic_prepare_civil_war

		available = {
			GER = {
				has_war_with = ENG
			}
		}

		fire_only_once = yes
		cost = 10
		ai_will_do = {
			factor = 500
		}

		visible = {
			tag = ENG
			is_subject = no
			has_war_with = GER
			PER = {
				exists = yes
				is_subject = no
				has_idea = PER_German_technology_group
			}
			GER = {
				exists = yes
				is_subject = no
			}
			NOT = {
				is_in_faction_with = GER
				is_in_faction_with = PER
				has_war_with = PER
			}
		}
		remove_effect = {
		}
		complete_effect = {
			PER = { country_event = { id = EFM_Persia.100 hours = 12 } }
		}
	}
	ENG_Require_persian_to_expel_germans = {

		icon = generic_prepare_civil_war

		available = {
			GER = {
				has_war_with = ENG
				has_war_with = SOV
			}
		}

		fire_only_once = yes
		cost = 10
		ai_will_do = {
			factor = 500
		}

		visible = {
			tag = ENG
			is_subject = no
			has_war_with = GER
			PER = {
				exists = yes
				is_subject = no
				has_idea = PER_German_technology_group
			}
			GER = {
				exists = yes
				is_subject = no
			}
			SOV = {
				exists = yes
				is_subject = no
			}
			NOT = {
				is_in_faction_with = GER
				is_in_faction_with = PER
				has_war_with = PER
				has_war_with = SOV
			}
		}
		remove_effect = {
		}
		complete_effect = {
			PER = { country_event = { id = EFM_Persia.101 hours = 12 } }
		}
	}
	ENG_Invade_persian = {

		icon = generic_operation

		available = {
		}

		fire_only_once = yes
		cost = 50
		days_remove = 35
		ai_will_do = {
			factor = 500
		}

		visible = {
			tag = ENG
			is_subject = no
			PER = {
				exists = yes
				is_subject = no
				has_idea = PER_German_technology_group
			}
			has_global_flag = Anglo_soviet_invasion_of_iran
			NOT = {
				is_in_faction_with = GER
				is_in_faction_with = PER
				has_war_with = PER
				has_war_with = SOV
			}
		}
		remove_effect = {
			country_event = { id = EFM_Persia.105 }
			declare_war_on = {
				target = PER
				type = puppet_wargoal_focus
			}
		}
		complete_effect = {
		}
	}
	
}
