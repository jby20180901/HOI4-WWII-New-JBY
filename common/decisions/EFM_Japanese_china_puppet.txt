political_actions = {

	CPG_Control_north_china = {

		icon = generic_oppression

		available = {
			has_war_with = event_target:WTT_current_china_leader
			JAP = { has_war_with = event_target:WTT_current_china_leader }
			any_state = { 
				is_fully_controlled_by = JAP
				is_claimed_by = CPG
			}
			NOT = { has_country_flag = JAP_Japan_transfer_state_to_us }
		}
		
		ai_will_do = {
			factor = 500
		}

		cost = 10

		visible = {
			tag = CPG
			has_war_with = event_target:WTT_current_china_leader
			JAP = {
					has_war_with = event_target:WTT_current_china_leader
					has_country_flag = use_chinese_puppet_against_chinese
				}
		}
		is_good = yes
		
		days_remove = 35

		remove_effect = {
			every_state = {
				limit = {
					is_controlled_by = JAP
					is_claimed_by = CPG
				}
				ROOT = { set_state_controller = PREV }
			}
		}
		cancel_trigger = {
			has_country_flag = JAP_Japan_transfer_state_to_us
		}
	}
	CNJ_Control_south_china = {

		icon = generic_oppression

		available = {
			has_war_with = event_target:WTT_current_china_leader
			JAP = {
					has_war_with = event_target:WTT_current_china_leader
					has_country_flag = use_chinese_puppet_against_chinese
				}
			any_state = { 
				is_fully_controlled_by = JAP
				is_claimed_by = CNJ
			}
			NOT = { has_country_flag = JAP_Japan_transfer_state_to_us }
		}
		
		ai_will_do = {
			factor = 500
		}

		cost = 10

		visible = {
			tag = CNJ
			has_war_with = event_target:WTT_current_china_leader
			JAP = {
					has_war_with = event_target:WTT_current_china_leader
					has_country_flag = use_chinese_puppet_against_chinese
				}
		}
		is_good = yes
		
		days_remove = 35

		remove_effect = {
			every_state = {
				limit = {
					is_controlled_by = JAP
					is_claimed_by = CNJ
				}
				ROOT = { set_state_controller = PREV }
			}
		}
		cancel_trigger = {
			has_country_flag = JAP_Japan_transfer_state_to_us
		}
	}
	
	JCP_Leader_china = {

		icon = generic_political_actions

		fire_only_once = yes
		available = {
			has_stability > 0.75
		}
		
		ai_will_do = {
			factor = 500
		}

		cost = 100

		visible = {
			has_completed_focus = JCP_Real_government
		}
		is_good = yes
		
		days_remove = 70

		remove_effect = {
			every_state = {
				limit = {
					is_core_of = PRC
					NOT = { is_core_of = ROOT }
				}
				add_core_of = ROOT
			}
		}
		cancel_trigger = {
			ROOT = {
				OR = {
					exists = no
					has_stability < 0.75
				}
			}
		}
	}

}