
operations = {

	SIK_Soviet_control_us_player = {

		icon = generic_civil_support

		available = {
			has_political_power > -1
		}

		days_mission_timeout = 5

		activation = {
			is_ai = no
			OR = {
				has_idea = SIK_Surrport
				has_idea = SIK_Weakened_ussr_influence
			}
			OR = {
				communism > 0.7
				has_political_power < 0
			}
			is_subject = no
			NOT = {
				has_war_with = SOV
			}
		}
		remove_trigger = { #will run remove effect and abort
			has_political_power > -1
		}	

		fixed_random_seed = no

		fire_only_once = no

		is_good = no

		timeout_effect = {
			country_event = { id = Sinkiang.0 }
		}
	}
	SIK_Soviet_control_us_ai = {

		icon = generic_civil_support

		available = {
			has_political_power > -39
		}

		days_mission_timeout = 10

		activation = {
			is_ai = no
			OR = {
				has_idea = SIK_Surrport
				has_idea = SIK_Weakened_ussr_influence
			}
			OR = {
				communism > 0.85
				has_political_power < -40
			}
			is_subject = no
			NOT = {
				has_war_with = SOV
			}
		}
		remove_trigger = { #will run remove effect and abort
			has_political_power > -39
		}	

		fixed_random_seed = no

		fire_only_once = no

		is_good = no

		timeout_effect = {
			country_event = { id = Sinkiang.11 }
		}
	}
	
}